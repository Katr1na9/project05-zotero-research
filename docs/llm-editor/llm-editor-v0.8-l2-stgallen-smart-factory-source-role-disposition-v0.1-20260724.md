# St.Gallen Smart Factory source-role disposition

## Verdict

**Hold at metadata-candidate level; do not approve a source role. Reject the consumed v0.1 audit path, not the underlying family.**

The single v0.1 execution ended with:

- status `failed_closed_terminal_no_automatic_retry`;
- reason `wall_time_cap_exceeded`;
- no raw manifest or sensor content persisted;
- no protected-manifest model visibility;
- no role or credit change.

The attempt is exhausted. Portfolio direction 04 remains vacant, pointer status remains `unbound`, and family, lineage, sample, and quota credit remain zero.

## Why this is a hold rather than approval or payload rejection

The terminal result contains no admissible aggregate findings for schema, privacy, notice, protected-ground-truth exclusion, manifest lineage, process-to-sensor binding, pointer recovery, semantic fit, or LLM increment.

Therefore:

- it cannot support source-role approval;
- it cannot be interpreted as zero privacy, notice, label, or schema matches;
- it also does not establish that the payload failed a substantive safety Gate.

What it does establish is narrower: the frozen v0.1 audit design did not complete within its pre-registered 300-second wall-time budget. That is terminal negative evidence about this audit method, not a scientific verdict on the underlying family.

## Disposition

St.Gallen remains:

- `metadata_candidate_hold_source_role_unresolved_attempt_exhausted`;
- outside the effective catalog;
- outside train and development;
- outside model-visible inputs;
- outside lineage, sample, quota, and pointer credit;
- unable to occupy portfolio direction 04.

The acquired sensor surface and protected manifest may not be reopened, reread, reparsed, joined, or used to generate any sample under this disposition.

## Forbidden rescue

Do not:

- retry, resume, restart, or relabel the consumed v0.1 attempt;
- raise only `maximum_wall_seconds` and rerun the same audit;
- reuse the v0.1 authority, script identity, contract status, result paths, or execution counter;
- remove, narrow, whitelist, defer, sample away, or reinterpret any privacy, notice, protected-label, ground-truth, schema, lineage, binding, pointer, or sanitization Gate;
- choose a smaller post-result row, field, time, process, activity, station, component, or sensor subset and call it equivalent;
- expose the protected manifest or any of its identifiers, intervals, activities, states, timestamps, pointers, or derived supervision to a model;
- convert the curator-declared ten executions or successful acquisition identity into lineage or sample credit.

## Conditions for any future attempt

A future attempt is not authorized here. It would require all of the following:

1. a new versioned method contract;
2. a newly pinned script identity and SHA-256;
3. new result paths;
4. a new single-use execution authority naming both targets;
5. a new explicit execute instruction;
6. a code-level root-cause analysis that does not reopen either source;
7. a redesigned pass structure with deterministic stage budgets;
8. synthetic-fixture evidence that timeout, aggregate-only output, protected-manifest invisibility, and fail-closed behavior work;
9. proof that no safety Gate was weakened or made sample-dependent.

The default wall-time cap should remain 300 seconds. Any cap change would need independent methodological justification and cannot be the sole amendment.

Even a future successful bounded audit would only reopen source-role review. It would not automatically award credit, create training data, bind pointers, write the catalog, or pass L2.

## Scope

This disposition used only the committed sanitized terminal result and prior committed contracts. Neither surface was reopened or read, no audit was rerun, no cap or parser changed, no catalog or role changed, no sample was generated, and no baseline, fine-tuning, Kernel, Gamma, M3, L2, or push action occurred.
