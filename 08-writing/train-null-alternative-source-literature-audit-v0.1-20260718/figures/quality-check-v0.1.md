# Figure quality check: `train-negative-unit-amendment-v0.1.svg`

Date: 2026-07-18  
Generation mode: deterministic SVG fallback because `OPENROUTER_API_KEY` was unavailable; no Nano Banana/Gemini score is claimed.

## Checks

- Vector SVG, 1400 × 860 view box; no raster compression.
- Arial/Helvetica fallback stack; minimum label size 14 px.
- Okabe–Ito-compatible palette with direct labels and shape/pattern redundancy.
- Rejection uses diagonal hatching; meaning does not depend on color alone.
- White background, dark text, consistent 2.2–3 px strokes.
- SVG contains `title` and `desc` accessibility elements.
- Caption in the review defines the meaning of `packet-null` and `pointer-bounded` negative.
- Layout separates the three evidence units from the five-stage pipeline and avoids crossing solid data-flow arrows.
- Rendered at 1400 × 860 with headless Microsoft Edge and visually inspected; labels remain inside their intended panels, pipeline arrows terminate correctly, and the retained preview is `train-negative-unit-amendment-v0.1-preview.png`.

## Limitation

The scientific-schematics AI generator and Gemini quality review could not run without the configured API key. This deterministic vector fallback follows the skill's publication/accessibility guidance but is not represented as an AI-generator quality-score pass.
