# Figure regeneration note

`prior-art-gap-map.svg` is the editable vector source. It follows the scientific-schematics accessibility rules: Okabe–Ito colors, direct labels, high contrast, and no color-only encoding.

The preferred generated deliverable is `prior-art-gap-map.png` at 300 DPI. The exact Nano Banana workflow could not run because `OPENROUTER_API_KEY` is not configured; the SVG is therefore an AI-authored deterministic fallback and should be regenerated with the scientific-schematics script if that key becomes available.
