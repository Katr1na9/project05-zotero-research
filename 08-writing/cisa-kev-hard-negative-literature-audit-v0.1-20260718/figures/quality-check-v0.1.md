# Figure quality check: `kev-evidence-unit-decision-v0.1.svg`

Date: 2026-07-18  
Generation mode: deterministic SVG fallback because `OPENROUTER_API_KEY` was unavailable; no Nano Banana/Gemini score is claimed.

## Checks

- Vector SVG, 1400 × 820 view box; no raster compression.
- Arial/Helvetica fallback stack; minimum label size 15 px.
- Okabe–Ito colors with direct text labels and shape/pattern redundancy.
- Rejection uses diagonal hatch; meanings do not depend on color alone.
- White background, dark text, consistent 2.2–3 px strokes.
- Rendered at 1400 × 820 with headless Microsoft Edge and visually inspected; all arrows terminate at intended boxes and no text/shape overlap remains. The retained preview is `kev-evidence-unit-decision-v0.1-preview.png`.
- SVG contains `title` and `desc` accessibility elements.
- Caption in the review defines KEV, PU and the Project05 null contract.

## Limitation

The scientific-schematics AI generator and its automated Gemini review could not run without the configured API key. This file follows that skill's publication and accessibility rules but is not represented as an AI-generator quality-score pass.
