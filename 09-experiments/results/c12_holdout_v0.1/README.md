# C12 frozen MVP evaluation v0.1

This directory contains one separate G1 operational stress case with 45 repeated mask/intensity/seed conditions. It must not be pooled with C07-C10 G3 cases or the C11 G2 emulation case.

M2 reaches the internal target in all 45 conditions at mean successful cost `1.4222`; Coverage, CMI and M3a also reach all conditions at `0.9778`. The result is a boundary/transfer result, not an actor-attribution accuracy result.
