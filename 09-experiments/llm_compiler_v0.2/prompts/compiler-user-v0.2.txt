The last user message is a JSON request with `stage="compiler"` and `packet` equal to the complete public context packet. Treat every other field as execution metadata, not evidence. Preserve literal source values and source pointers. Return only the compiler JSON object required by the system message.
